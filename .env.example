# Copia este fichero como .env y rellena los valores reales
# El .env nunca se sube al repo ni al servidor de control de versiones

DB_HOST=127.0.0.1
DB_PORT=3307
DB_USER=tu_usuario_mariadb
DB_PASS=tu_contraseña_mariadb
DB_NAME=shifter_sync
